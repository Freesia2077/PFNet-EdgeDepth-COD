name: pfnet-cod
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - pip
  - numpy
  - scipy
  - matplotlib
  - pillow
  - tqdm
  - pip:
      - torch
      - torchvision
      - opencv-python
      - tensorboardX
      - timm
      - einops
